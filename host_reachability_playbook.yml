---
# ======================================
# Host Reachability Monitoring Playbook
# ======================================
# This playbook checks if all hosts in the zammad_servers group are reachable
# and sends email alerts via KIT SMTP server (smtp.kit.edu).
# 
# Run every 2 hours via CronJob or AWX schedule
# ======================================

- name: Host Reachability Monitoring and Alerting
  hosts: localhost
  gather_facts: yes
  connection: local
  become: no
  
  vars:
    # SMTP Configuration - Use smarthost.kit.edu (no auth needed from KIT network)
    # OR use smtp.kit.edu with authentication
    smtp_host: "{{ smtp_host | default('smarthost.kit.edu') }}"
    smtp_port: "{{ smtp_port | default(25) }}"
    # smtp_username: "{{ smtp_username | default('') }}"
    # smtp_password: "{{ smtp_password | default('') }}"
    smtp_use_tls: "{{ smtp_use_tls | default(false) }}"
    
    # Email Configuration
    email_from: "{{ smtp_username }}@kit.edu"  # Use authenticated user's email
    alert_recipients: ["{{ smtp_username }}@kit.edu"]  # Change to desired alert recipients
    test_email_recipient: "lisana.berberi@kit.edu"  # Change this to your email for testing
    
    # Monitoring Configuration
    check_port: 22
    check_timeout: 5
    alert_on_unreachable: true

        # Disk monitoring configuration
    disk_usage_threshold: 95  # Alert if usage > 95%
    
    # Certificate monitoring configuration
    cert_expiry_warning_days: 30  # Alert if expires in < 30 days
    cert_paths:  # Paths to check for SSL certificates
      - "/etc/ssl/certs"
      - "/etc/letsencrypt/live"
    
    # Report settings
    report_dir: "/runner/artifacts"
    
  pre_tasks:
    - name: Display inventory hosts
      debug:
        msg: |
          Monitoring the following hosts from inventory:
          {% for host in groups['zammad_servers'] %}
          - {{ host }}
            IP: {{ hostvars[host].ansible_host | default('N/A') }}
            State: {{ hostvars[host].state | default('N/A') }}
            Owner: {{ hostvars[host].owner | default('N/A') }}
          {% endfor %}

  tasks:
    - name: Create artifacts directory
      file:
        path: "{{ report_dir }}"
        state: directory
        mode: '0755'

    - name: Initialize reachability report
      set_fact:
        host_reachability:
          timestamp: "{{ now(utc=True).isoformat() }}"
          check_port: "{{ check_port }}"
          reachable_hosts: []
          unreachable_hosts: []
          total_checked: "{{ groups['zammad_servers'] | length }}"
          hosts_detail: {}

    # ================================
    # TASK 1: CHECK HOST REACHABILITY
    # ================================
    - name: Check connectivity to all hosts
      wait_for:
        host: "{{ hostvars[item].ansible_host | default(item) }}"
        port: "{{ check_port }}"
        timeout: "{{ check_timeout }}"
        state: started
      loop: "{{ groups['zammad_servers'] }}"
      register: reachability_check
      ignore_errors: yes
      changed_when: false

    - name: Parse reachability results with host details
      set_fact:
        host_reachability: >-
          {{
            host_reachability | combine({
              'reachable_hosts': host_reachability.reachable_hosts + ([] if item is failed else [item.item]),
              'unreachable_hosts': host_reachability.unreachable_hosts + ([] if item is succeeded else [item.item]),
              'hosts_detail': host_reachability.hosts_detail | combine({
                item.item: {
                  'ip': hostvars[item.item].ansible_host | default('N/A'),
                  'state': hostvars[item.item].state | default('N/A'),
                  'owner': hostvars[item.item].owner | default('N/A'),
                  'reachable': item is succeeded
                }
              })
            })
          }}
      loop: "{{ reachability_check.results }}"
      loop_control:
        label: "{{ item.item }}"

    - name: Display reachability summary
      debug:
        msg: |
          =========================
          Reachability Check Summary
          =========================
          Timestamp: {{ host_reachability.timestamp }}
          Total Hosts: {{ host_reachability.total_checked }}
          Reachable: {{ host_reachability.reachable_hosts | length }}
          Unreachable: {{ host_reachability.unreachable_hosts | length }}
          
          REACHABLE HOSTS:
          {% for host in host_reachability.reachable_hosts | sort %}
          ✓ {{ host }}
            IP: {{ host_reachability.hosts_detail[host].ip }}
            State: {{ host_reachability.hosts_detail[host].state }}
            Owner: {{ host_reachability.hosts_detail[host].owner }}
          {% endfor %}
          
          {% if host_reachability.unreachable_hosts %}
          UNREACHABLE HOSTS:
          {% for host in host_reachability.unreachable_hosts | sort %}
          ✗ {{ host }}
            IP: {{ host_reachability.hosts_detail[host].ip }}
            State: {{ host_reachability.hosts_detail[host].state }}
            Owner: {{ host_reachability.hosts_detail[host].owner }}
          {% endfor %}
          {% endif %}



    # ================================
    # TASK 2: CHECK DISK SPACE
    # ================================
    - name: Check disk space on reachable hosts
      shell: |
        df -h / | tail -1 | awk '{print $5}' | sed 's/%//'
      register: disk_usage_check
      loop: "{{ monitoring_report.reachability.reachable_hosts }}"
      loop_control:
        label: "{{ item }}"
      delegate_to: "{{ item }}"
      ignore_errors: yes
      changed_when: false

    - name: Parse disk space results
      set_fact:
        monitoring_report: >-
          {{
            monitoring_report | combine({
              'disk_space': monitoring_report.disk_space | combine({
                'checked_hosts': monitoring_report.disk_space.checked_hosts + [item.item],
                'high_usage_hosts': monitoring_report.disk_space.high_usage_hosts + ([] if (item.stdout | int(0) <= disk_usage_threshold) else [item.item]),
                'hosts_detail': monitoring_report.disk_space.hosts_detail | combine({
                  item.item: {
                    'usage_percent': item.stdout | int(0),
                    'threshold': disk_usage_threshold,
                    'alert': (item.stdout | int(0) > disk_usage_threshold)
                  }
                })
              })
            })
          }}
      loop: "{{ disk_usage_check.results }}"
      loop_control:
        label: "{{ item.item }}"
      when: item is not skipped

    # ================================
    # TASK 3: CHECK CERTIFICATE EXPIRY
    # ================================
    - name: Find certificate files on reachable hosts
      find:
        paths: "{{ cert_paths }}"
        patterns: "*.crt,*.pem"
        file_type: file
      register: cert_files_check
      loop: "{{ monitoring_report.reachability.reachable_hosts }}"
      loop_control:
        label: "{{ item }}"
      delegate_to: "{{ item }}"
      ignore_errors: yes
      changed_when: false

    - name: Check certificate expiry dates
      shell: |
        {% set certs = item.files | default([]) %}
        {% if certs | length > 0 %}
        {% for cert in certs %}
        openssl x509 -in "{{ cert.path }}" -noout -enddate 2>/dev/null || true
        {% endfor %}
        {% endif %}
      register: cert_expiry_check
      loop: "{{ cert_files_check.results }}"
      loop_control:
        label: "{{ item.item }}"
      delegate_to: "{{ item.item }}"
      ignore_errors: yes
      changed_when: false
      when: item is not skipped

    - name: Parse certificate expiry results
      set_fact:
        cert_expiry_data: "{{ cert_expiry_data | default({}) | combine({
          item.item: {
            'expiry_output': item.stdout_lines | default([]),
            'has_certs': (item.stdout_lines | length) > 0
          }
        }) }}"
      loop: "{{ cert_expiry_check.results }}"
      loop_control:
        label: "{{ item.item }}"
      when: item is not skipped

    - name: Process certificate expiry dates
      set_fact:
        cert_info: "{{ lookup('template', 'cert_expiry_parser.j2') }}"
      ignore_errors: yes

    - name: Update monitoring report with certificate data
      set_fact:
        monitoring_report: >-
          {{
            monitoring_report | combine({
              'certificates': {
                'checked_hosts': monitoring_report.reachability.reachable_hosts,
                'expiring_soon': cert_info.expiring_soon | default([]),
                'expired': cert_info.expired | default([]),
                'hosts_detail': cert_info.hosts_detail | default({})
              }
            }, recursive=True)
          }}
      when: cert_info is defined


    # ================================
    # ALERTING SECTION
    # ================================
    - name: Send alert email - Unreachable hosts detected
      community.general.mail:
        host: "{{ smtp_host }}"
        port: "{{ smtp_port }}"
        secure: "never"
        from: "{{ email_from }}"
        to: "{{ alert_recipients }}"
        subject: "🚨 ALERT: {{ host_reachability.unreachable_hosts | length }} host(s) unreachable - {{ host_reachability.timestamp }}"
        body: |
          HOST REACHABILITY ALERT
          =======================
          
          Timestamp: {{ host_reachability.timestamp }}
          
          STATUS: UNREACHABLE HOSTS DETECTED
          
          {{ host_reachability.unreachable_hosts | length }} out of {{ host_reachability.total_checked }} hosts are currently unreachable.
          
          UNREACHABLE HOSTS:
          {% for host in host_reachability.unreachable_hosts | sort %}
          • {{ host }} ({{ host_reachability.hosts_detail[host].ip }})
            State: {{ host_reachability.hosts_detail[host].state }}
            Owner: {{ host_reachability.hosts_detail[host].owner }}
          {% endfor %}
          
          REACHABLE HOSTS:
          {% for host in host_reachability.reachable_hosts | sort %}
          • {{ host }} ({{ host_reachability.hosts_detail[host].ip }})
            State: {{ host_reachability.hosts_detail[host].state }}
            Owner: {{ host_reachability.hosts_detail[host].owner }}
          {% endfor %}
          
          SUMMARY:
          - Total Hosts: {{ host_reachability.total_checked }}
          - Reachable: {{ host_reachability.reachable_hosts | length }}
          - Unreachable: {{ host_reachability.unreachable_hosts | length }}
          - Check Method: SSH Port {{ host_reachability.check_port }}
          
          Please investigate the unreachable hosts immediately.
          
          Regards,
          Automated Monitoring System
      when:
        - host_reachability.unreachable_hosts | length > 0
      ignore_errors: yes

    - name: Send test email - For verification (optional)
      community.general.mail:
        host: "{{ smtp_host }}"
        port: "{{ smtp_port }}"
        secure: "never"
        from: "{{ email_from }}"
        to: ["{{ test_email_recipient }}"]
        subject: "Test: Host Monitoring System - {{ host_reachability.timestamp }}"
        body: |
          This is a test email from the Host Monitoring System.
          
          All {{ host_reachability.total_checked }} hosts are currently reachable:
          
          {% for host in host_reachability.reachable_hosts | sort %}
          ✓ {{ host }} ({{ host_reachability.hosts_detail[host].ip }})
          {% endfor %}
          
          If you received this email, the monitoring system is working correctly.
          
          Regards,
          Automated Monitoring System
      ignore_errors: yes    

    - name: Set play result
      set_fact:
        monitoring_result:
          status: "{% if host_reachability.unreachable_hosts | length == 0 %}success{% else %}warning{% endif %}"
          unreachable_count: "{{ host_reachability.unreachable_hosts | length }}"
          reachable_count: "{{ host_reachability.reachable_hosts | length }}"


    - name: Send alert email - High disk usage detected
      community.general.mail:
        host: "{{ smtp_host }}"
        port: "{{ smtp_port }}"
        secure: "never"
        from: "{{ email_from }}"
        to: "{{ alert_recipients }}"
        subject: "⚠️ ALERT: {{ monitoring_report.disk_space.high_usage_hosts | length }} host(s) with high disk usage (>{{ disk_usage_threshold }}%) - {{ monitoring_report.timestamp }}"
        body: |
          DISK SPACE ALERT
          ================
          
          Timestamp: {{ monitoring_report.timestamp }}
          
          STATUS: HIGH DISK USAGE DETECTED
          
          {{ monitoring_report.disk_space.high_usage_hosts | length }} host(s) have disk usage exceeding {{ disk_usage_threshold }}%.
          
          HOSTS WITH HIGH DISK USAGE:
          {% for host in monitoring_report.disk_space.high_usage_hosts | sort %}
          • {{ host }}
            Current Usage: {{ monitoring_report.disk_space.hosts_detail[host].usage_percent }}%
            Threshold: {{ disk_usage_threshold }}%
            IP: {{ monitoring_report.reachability.hosts_detail[host].ip }}
            Owner: {{ monitoring_report.reachability.hosts_detail[host].owner }}
          {% endfor %}
          
          Please clean up disk space on these hosts.
          
          Regards,
          Automated Monitoring System
      when:
        - monitoring_report.disk_space.high_usage_hosts | length > 0
      ignore_errors: yes

    - name: Send alert email - Certificate expiry warning
      community.general.mail:
        host: "{{ smtp_host }}"
        port: "{{ smtp_port }}"
        secure: "never"
        from: "{{ email_from }}"
        to: "{{ alert_recipients }}"
        subject: "📅 ALERT: Certificates expiring soon or expired - {{ monitoring_report.timestamp }}"
        body: |
          CERTIFICATE EXPIRY ALERT
          ========================
          
          Timestamp: {{ monitoring_report.timestamp }}
          
          STATUS: CERTIFICATE EXPIRY DETECTED
          
          {% if monitoring_report.certificates.expired | length > 0 %}
          EXPIRED CERTIFICATES:
          {% for host in monitoring_report.certificates.expired | sort %}
          • {{ host }}
            Details: {{ monitoring_report.certificates.hosts_detail[host] | default('N/A') }}
          {% endfor %}
          
          {% endif %}
          {% if monitoring_report.certificates.expiring_soon | length > 0 %}
          CERTIFICATES EXPIRING SOON (within {{ cert_expiry_warning_days }} days):
          {% for host in monitoring_report.certificates.expiring_soon | sort %}
          • {{ host }}
            Details: {{ monitoring_report.certificates.hosts_detail[host] | default('N/A') }}
          {% endfor %}
          
          {% endif %}
          Please renew these certificates immediately.
          
          Regards,
          Automated Monitoring System
      when:
        - (monitoring_report.certificates.expired | length > 0) or (monitoring_report.certificates.expiring_soon | length > 0)
      ignore_errors: yes

    - name: Display comprehensive monitoring summary
      debug:
        msg: |
          ========================================
          COMPREHENSIVE MONITORING SUMMARY
          ========================================
          Timestamp: {{ monitoring_report.timestamp }}
          
          1. REACHABILITY CHECK
          --------------------
          Total Hosts: {{ monitoring_report.reachability.total_checked }}
          Reachable: {{ monitoring_report.reachability.reachable_hosts | length }}
          Unreachable: {{ monitoring_report.reachability.unreachable_hosts | length }}
          
          2. DISK SPACE CHECK
          -------------------
          Hosts Checked: {{ monitoring_report.disk_space.checked_hosts | length }}
          High Usage (>{{ disk_usage_threshold }}%): {{ monitoring_report.disk_space.high_usage_hosts | length }}
          
          3. CERTIFICATE CHECK
          --------------------
          Hosts Checked: {{ monitoring_report.certificates.checked_hosts | length }}
          Expired: {{ monitoring_report.certificates.expired | length }}
          Expiring Soon (<{{ cert_expiry_warning_days }} days): {{ monitoring_report.certificates.expiring_soon | length }}
          
          ========================================